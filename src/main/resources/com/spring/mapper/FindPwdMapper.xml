<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.spring.mapper.FindPwdMapper">
  <select id="findPwd" resultType="string">
  		SELECT COUNT(*) FROM  members WHERE (name, phone,email) IN ( (#{name,jdbcType=VARCHAR}, #{phone,jdbcType=VARCHAR}, #{email,jdbcType=VARCHAR}) )
  	</select>
  	<update id="updatePwd">
  	UPDATE members SET pwd = #{tempPwd} WHERE email = #{email}
  	</update>
  	</mapper>