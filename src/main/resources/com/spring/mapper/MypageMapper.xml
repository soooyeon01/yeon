<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.spring.mapper.MypageMapper">
  <select id="selectMypage" resultType="com.spring.MembersDTO">
  		SELECT email
  				, pwd
  				, phone
  				, name
  				, nickname 
  				FROM members WHERE email = #{email}
  	</select>
  	<update id="UpdateMypage">
		UPDATE members 
			SET name = #{name}
				, pwd = #{pwd} 
				, phone = #{phone} 
				, nickname = #{nickname} 
			WHERE email = #{email}	
  	</update>
  	
  	</mapper>